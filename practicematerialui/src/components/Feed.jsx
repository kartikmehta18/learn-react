import { makeStyles } from "@mui/material";

const useStyles = makeStyles((theme) => ({}));

const Feed =() =>{
    const classes =useStyles();
    return(
        <div>
            <h1>Feed</h1>
        </div>
    )
};
export default Feed;